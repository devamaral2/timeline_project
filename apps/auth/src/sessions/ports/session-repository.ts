import type { AuditEventInput } from "../../audit/audit-event"; import type { RequestContext } from "../../common/request-context"; import type { SignAccessToken } from "../../crypto/jwt"; import type { AuthenticatedActor, ResolvedAccess } from "../../users/user"; import type { Session } from "../session";
export interface RotateRefreshTokenCommand {presentedTokenHash:string;successor:{id:string;hash:string;issuedAt:Date;expiresAt:Date};now:Date;context:RequestContext;auditEvents:readonly AuditEventInput[]}
export interface SessionRepository {rotateRefreshToken(c:RotateRefreshTokenCommand,sign:SignAccessToken):Promise<{kind:"rotated";accessToken:string;session:Session;access:ResolvedAccess;refreshTokenExpiresAt:Date}|{kind:"reused"}|{kind:"invalid"}>;revokeByRefreshToken(c:{presentedTokenHash:string;now:Date;context:RequestContext;auditEvents:readonly AuditEventInput[]}):Promise<boolean>;revokeAllOfUser(c:{actor:AuthenticatedActor;now:Date;context:RequestContext;auditEvents:readonly AuditEventInput[]}):Promise<number>}
