{
	"$schema": "https://biomejs.dev/schemas/2.5.13/schema.json",
	"extends": "//",
	"linter": {
		"rules": {
			"style": {
				"noRestrictedImports": {
					"level": "error",
					"options": {
						"paths": {
							"@repo/persistence": "apps/web nao pode importar @repo/persistence — so apps/api fala com o Postgres (ver AGENTS.md).",
							"@repo/entities": "apps/web nao compartilha entidades do backend; use os contratos HTTP locais."
						},
						"patterns": [
							{
								"group": ["@repo/entities/*"],
								"message": "apps/web nao compartilha entidades do backend; use os contratos HTTP locais."
							}
						]
					}
				}
			}
		}
	}
}
