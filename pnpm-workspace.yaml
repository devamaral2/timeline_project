packages:
  - "apps/*"
  - "packages/*"

# Dependencias autorizadas a rodar scripts de instalacao. esbuild (via vitest) e
# protobufjs (via firebase-admin) precisam disso para linkar/gerar seus binarios.
allowBuilds:
  "@firebase/util": true
  "@openrouter/sdk": true
  esbuild: true
  protobufjs: true
