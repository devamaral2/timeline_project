name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_call:

permissions:
  contents: read

jobs:
  transcription:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install locked transcription dependencies
        run: python -m pip install --require-hashes -r services/transcription/requirements.txt
      - name: Test jobs, HTTP boundary and audio decoding (no GPU required)
        run: PYTHONPATH=services/transcription python -m unittest discover -s services/transcription/tests -v
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Enable pnpm
        run: corepack enable
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Lint (existing baseline is non-blocking)
        # The current web baseline has unrelated Biome violations. Keep the
        # signal visible without preventing the first production pipeline from
        # deploying until that baseline is cleaned up separately.
        continue-on-error: true
        run: pnpm lint
      - name: Typecheck
        run: pnpm typecheck
      - name: Unit tests
        run: pnpm run --silent test:unit:ai
      - name: Build
        run: pnpm exec turbo run build
      - name: Install Playwright browser
        run: pnpm exec playwright install --with-deps chromium
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install worker dependencies for the real E2E HTTP service
        run: python -m pip install --require-hashes -r services/transcription/requirements.txt
      - name: E2E (isolated PostgreSQL and mocked external services)
        run: pnpm run --silent test:e2e:ai
